# Adult Hygiene terminology review

This follow-up records terminology that still needs human confirmation after
expanding the requested Adult Hygiene starter labels.

## Requested source labels not present

The following source labels were not present in the application, tests,
documentation, legacy files, ignored files, or Git history at the time of this
review. No new catalogue choices were invented for them. A human should confirm
whether these are missing starter choices that should be added and, if so, to
which catalogue:

- `REC USING ETB` (requested replacement: `RECOMMENDED USING ETB`)
- `FLUORIDE RINSE + PREVIDENT TP` (requested replacement: `FLUORIDE RINSE AND PREVIDENT TOOTHPASTE`)
- `BRUSHING- ?x/day` variants (requested replacement: `BRUSHING ?x/day`)

The existing brushing suggestions already use the requested form—`Brushing
1x/day`, `Brushing 2x/day`, and `Brushing 3x/day`—so they were left unchanged.

## Residual abbreviations and product terminology

The following terms remain in Adult Hygiene catalogue labels or template copy
and should be checked by a clinician or product owner before any expansion:

| Term | Current context | Human check |
| --- | --- | --- |
| `BW`, `PA` | Radiograph choices | Confirm these should remain standard clinical abbreviations. |
| `FMP` | Periodontal charting and treatment choices | Confirm the intended expansion and whether expanding it would reduce familiarity. |
| `NP` | Medical-history starter label | Confirm the intended meaning and preferred patient-record wording. |
| `OHI` / `OH` | Oral-hygiene sections and labels | Confirm whether these should be expanded in clinician-facing copy. |
| `PSR` | Periodontal screening label | Confirm whether the full assessment name is preferred. |
| `EOE` / `IOE` / `WNL` | Examination labels and summary text | Confirm whether generated notes must retain these abbreviations. |
| `LA` / `C/I` | Local-anesthetic template copy | Confirm the intended expansion and clinical notation requirements. |
| `CPAP` | Medical-history and sleep-apnea fields | Confirm this familiar device acronym should remain. |
| `RDA` / `RDH` | Visit-team role labels | Confirm role abbreviations are appropriate for all supported jurisdictions. |
| `1U`–`4U` | Scaling and polishing treatment choices | Confirm that `U` unambiguously means units in the target clinic. |
| `FL`, `VOCO`, `PREVIDENT`, `Crystal X-PUR` | Desensitizer and product choices | Confirm capitalization, product naming, and whether `FL` should be expanded. |
| `ETB` | Requested but currently absent `RECOMMENDED USING ETB` label | If this choice is added, confirm whether `ELECTRIC TOOTHBRUSH` should be written in full for consistency. |
