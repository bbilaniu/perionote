---
"hygienenote": patch
---

Keep saved-draft lists synchronized with browser storage through subscriptions, including saves and deletions in the same tab or another tab. Read cached metadata snapshots during rendering and prune expired or invalid records outside rendering.
